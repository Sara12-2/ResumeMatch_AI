scikit-learn>=1.5,<1.6
numpy>=1.26,<2.0
pypdf>=5.0,<6.0
spacy>=3.7,<3.8
# Pinned: an unpinned spacy install can pull in a typer/click combo that
# breaks `spacy download` (typer>=0.10 dropped the click APIs spacy 3.7 uses).
typer>=0.3,<0.10
click>=8.0,<9.0
pytest>=8.0,<9.0
Flask>=3.0,<3.1
